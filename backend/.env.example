# Database (use postgres service hostname when running via Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/event_notification_db

# Security
SECRET_KEY=dev-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Optional: auto-seed on container startup (Docker only)
SEED_ON_STARTUP=true
