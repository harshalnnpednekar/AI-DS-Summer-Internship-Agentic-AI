# Ensure shell scripts always have LF line endings on all platforms
*.sh text eol=lf

# Dockerfiles should also use LF
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# Python files
*.py text eol=lf

# Default behaviour: auto-detect text/binary
* text=auto
